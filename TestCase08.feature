
Feature: Verify Out Specification 

  
Scenario: Test Verify Out Specification 
  
  
When user is on home page to Verify Out Specification 
When The user clicks on the Add Icon to start the process Verify Out Specification 
Then The user clicks on the Add Characteristic link to open the form Verify Out Specification 
Then The user Expands the Data Group Name dropdown to view the list of data groups Verify Out Specification 
Then The user Selects the desired data group from the dropdown Verify Out Specification 
Then The user Opens the Part dropdown to view the list of available parts Verify Out Specification 
Then The user Selects the appropriate part from the dropdown Verify Out Specification  
Then The userEnters the Characteristic Name in the respective input field Verify Out Specification 
Then The user Clicks the Add button to confirm the characteristic details Verify Out Specification 
And The user Inputs the Upper Spec value in the corresponding input field Verify Out Specification 
And The user Provides the Lower Spec value in the respective input field Verify Out Specification 
Then the userSpecifies the Subgroup Size to define grouping Verify Out Specification 
Then the user The user clicks the Save button to Verify Out Specification 
Then the user The user clicks the Close button to Verify Out Specification 


#creatig parameters defaut value


And the user navigates to Settings by clicking the Configuration option  Verify Out Specification 
When the user initiates the parameter creation process by clicking on the Add Parameter Verify Out Specification 
Then the user enters a name in the Parameter Name input field  Verify Out Specification 
Then the user Enters default value to Verify Out Specification 
And the user saves the parameter configuration by clicking the Save button  Verify Out Specification 
Then the user finalizes the process by clicking the Close button  to Verify Out Specification 																																				rify Out Specification 

#creatig parameter to always clear field

And the user navigates to Settings by clicking the Configuration option to Verify Out Specification 
When the user initiates the parameter creation process by clicking on the Add Parameter Verify Out Specification 
Then the user enters a name in the Parameter Name input field to Verify Out Specification 
Then the user click on always clear field check box to Verify Out Specification 
And the user saves the parameter configuration by clicking the Save button to Verify Out Specification 
Then the user finalizes the process by clicking the Close button to Verify Out Specification 





#assign para to paramters



Then the user clicks on the Files icon to initiate the process of adding a parameter to the part  Verify Out Specification 
Then the user clicks on the Add icon to start the parameter addition process  Verify Out Specification 
Then the user verifies the assignment by clicking on the Assign Parameter to Part link  Verify Out Specification 
Then the user clicks on the Group dropdown to view the available groups  Verify Out Specification 
Then the user selects the data group from the dropdown to specify the group  Verify Out Specification 
Then the user clicks on the Part dropdown to view the available parts  Verify Out Specification 
Then the user selects the part to which the parameter will be added  Verify Out Specification 
And the user drags and drops the previously used parameters into the part  Verify Out Specification 
Then the user drags and drops the required parameter into the part  Verify Out Specification 
Then the user confirms the addition of the parameter by clicking on the Save button  Verify Out Specification 
Then the user exits the parameter addition process by clicking the Close button  Verify Out Specification 




#Editing a Sequence

Then the user clicks on the Files button to navigate to the sequence section Verify Out Specification 
Then the user clicks on the expand button next to the Files icon to reveal more options Verify Out Specification 
Then the user selects a group name from the grid by clicking on it Verify Out Specification 
Then the user clicks on the arrow icon to expand the available options Verify Out Specification 
Then the user clicks on the Sequence link to open the sequence configuration page Verify Out Specification 
Then the user right-clicks on the sequence name to access additional options Verify Out Specification 
Then the user selects Edit Sequence by clicking on it Verify Out Specification 
Then the user rearranges the characteristics by dragging and dropping them into the desired order Verify Out Specification 
Then the user saves the changes by clicking the Save button Verify Out Specification 
Then the user closes the sequence configuration by clicking the Close button Verify Out Specification 

  